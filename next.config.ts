import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // ESLint is run separately; skip it during the build step
  // TypeScript errors are still enforced
};

export default nextConfig;
